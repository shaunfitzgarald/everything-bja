rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is an admin
    function isAdmin() {
      return request.auth != null && 
        exists(/databases/$(database)/documents/admin/allowlist) &&
        (request.auth.token.email.lower() in get(/databases/$(database)/documents/admin/allowlist).data.emails ||
         request.auth.token.email in get(/databases/$(database)/documents/admin/allowlist).data.emails);
    }

    // Public read access for most collections
    match /links/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /videos/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /credits/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /config/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Admin allowlist - allow ANYONE to read for debugging
    match /admin/allowlist {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /press_photos/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /characters/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /conversations/{document=**} {
      allow read: if isAdmin();
      allow create: if true; // Let anyone start a chat session
      allow update, delete: if isAdmin();
    }

    match /analytics_events/{document=**} {
      allow read: if isAdmin();
      allow create: if true;
      allow update, delete: if isAdmin();
    }

    match /analytics_stats/{document=**} {
      allow read: if true; // Let everyone see stats if we want to show "Popular" badges
      allow create, update, delete: if true; // Incrementing needs public access or a function
    }
    
    // Public write access for contact form
    match /contacts/{document=**} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }
  }
}
